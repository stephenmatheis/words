import { } from 'react';
import Link from 'next/link';
import styles from '@/styles/blog.module.scss';

export default function Blog({ }) {
    return (
        <div id={styles['blog']}>
            <h1>Blog</h1>
        </div>
    );
}
